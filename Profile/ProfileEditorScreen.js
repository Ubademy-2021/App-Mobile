import * as React from 'react'

import {
    NativeBaseProvider,
    Box,
    Heading,
    VStack,
    FormControl,
    Input,
    Button,
    ScrollView, Text
} from 'native-base'



export default function ProfileEditionForm ({ navigation }) {
    return (
        <NativeBaseProvider>
            <Text> Hola </Text>
        </NativeBaseProvider>
    )
}

